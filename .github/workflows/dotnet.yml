name: .NET

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: checkout-code
    - uses: actions/checkout@v3
  
    - name: setup-dotnet
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: 6.0.x
        

